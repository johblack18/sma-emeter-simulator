services:
  sma-emeter-simulator:
    build: https://github.com/johblack18/sma-emeter-simulator
    image: sma-emeter-simulator:latest
    container_name: sma-emeter-simulator
    restart: unless-stopped
    # multicast emeter packets must reach the LAN, so the container uses the host's network stack
    network_mode: host
